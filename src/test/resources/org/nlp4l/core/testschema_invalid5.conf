// Invalid schema. The mandatory path "schema.fields[N].name" contains no elements.
schema {
  defAnalyzer : org.apache.lucene.analysis.standard.StandardAnalyzer
  fields = [
    {
      indexed : true
    }
  ]
}